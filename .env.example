# Server-side API key from https://spoonacular.com/food-api
SPOONACULAR_API_KEY=

# Public production origin used by metadata, canonical links, and invitation links.
NEXT_PUBLIC_APP_URL=https://kökshyllan.se

# Server-only Resend credentials. Never prefix these with NEXT_PUBLIC_.
RESEND_API_KEY=
RESEND_FROM_EMAIL=Kökshyllan <inbjudan@kökshyllan.se>

# Server-only Supabase Admin credential used for account deletion. Never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
